# Archived Admin Dashboard (1990-admin)

This is a backup of the standalone Admin app formerly at `1919-admin/`.

**Reason:** All Admin features were migrated into `1990-pos` (POS + Management).

**Safe to delete later** once you confirm POS management covers your workflows.

Note: `node_modules` and `dist` were excluded from this archive copy to keep it smaller. Restore with `npm install` if you ever need to run this backup.
